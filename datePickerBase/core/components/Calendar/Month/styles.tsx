import styled from 'styled-components';
import Elevation from '../../../../../../foundations/elevations';

const MonthWrapper = styled(Elevation)`
  margin-right: 1rem;
  background: white;
  width: 288px; /* Equivalent to min width cellphones (without gap) */
`;

export default MonthWrapper;
